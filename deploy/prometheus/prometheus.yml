global:
  scrape_interval: 15s
  evaluation_interval: 30s
  scrape_timeout: 10s
  external_labels:
    monitor: vllm-observability
    env: prod

rule_files:
  - /etc/prometheus/recording_rules.yml
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: vllm
    honor_timestamps: true
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - 10.0.0.11:8000
        labels:
          service: vllm
          component: inference
          env: prod
          region: tw
          instance_name: gpt-oss-120b-a

      - targets:
          - 10.0.0.12:8000
        labels:
          service: vllm
          component: inference
          env: prod
          region: tw
          instance_name: gpt-oss-120b-b

      - targets:
          - 10.0.0.13:8000
        labels:
          service: vllm
          component: inference
          env: prod
          region: tw
          instance_name: gpt-oss-120b-c

  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090
        labels:
          service: prometheus
          component: monitoring
          env: prod
          region: tw